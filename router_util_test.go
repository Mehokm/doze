package rest

import (
	"testing"
)

func TestMe(t *testing.T) {
	// u1 := &Uri{[]rune("/test/path/1"), 0}
	// u2 := &Uri{[]rune("/test/path/long/2"), 0}

	// for u1.hasNext() && u2.hasNext() {
	// 	fmt.Println(u1.next(), u2.next())
	// }

	// u1 := NewUri("/people/{id:i}/details/{name:an}")
	// u2 := NewUri("/people/432/details/sdff3")
	//
	// um := UriMatcher{u1, u2}
	//
	// fmt.Println(um.match())
}
